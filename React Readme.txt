1. install the node modules(npm i @node_modules)
2. Firstly, run the Json server cmd in one terminal (npx json-server --watch src/Data/db.json --port 8000).
3. and run the npm start cmd in another terminal.
4. these are the id: Sai.atos@1 & password: Password@1 for application login page.
